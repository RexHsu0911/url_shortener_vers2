<div class="container">
  <p class="h1 text-center mt-5">URL Shortener</p>
  <div class="d-flex justify-content-center">
    <img src="/images/ExportedContentImage_00.png" style="width: 10rem" class="my-5" alt="link_image">
  </div>

  {{!-- action 表單提交時會送往的位址 --}}
  {{!-- method 定義 HTTP 的方法(get 或 post)發送數據(預設是 get) --}}
  <form class="d-grid gap-4 my-4" action="/shortUrls" method="POST">
    <label class="visually-hidden" for="inputUrl">Input URL here</label>
    <input type="url" class="form-control mx-auto" style="width: 36rem;" name="inputUrl" id="inputUrl"
      placeholder="Input URL here">
    <button class="d-grid gap-2 mx-auto btn btn-outline-primary" type="submit" id="shorten-url"
      style="width: 12rem;">shorten</button>
  </form>

  <hr>
  </hr>

  <div class="col-8 my-4 mx-auto">
    <p class="h3 text-center mt-5">Success! Please use this link:</p>
    <p id="show-url" class="text-primary text-center fw-bold">shortenUrl</p>
    <button type="submit" class="d-grid gap-2 mx-auto btn btn-primary" id="url-copy" style="width: 6rem;">Copy</button>
  </div>

  <hr>
  </hr>

  <p class="h2 text-center mt-5">Short URL Records</p>
  <table class="table table-striped table-responsive">
    <thead>
      <tr>
        <th>#</th>
        <th>INPUT</th>
        <th>OUTPUT</th>
      </tr>
    </thead>
    <tbody id="url-record">
      <tr>
        <th>1</th>
        <td>fullUrl</td>
        <td>https://.../12345</a></td>
      </tr>

      <tr>
        <th>2</th>
        <td>https://www.google.com</td>
        <td>https://.../12345</a></td>
      </tr>

    </tbody>
  </table>
</div>